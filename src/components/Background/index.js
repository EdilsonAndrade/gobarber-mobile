import LienarGradient from 'react-native-linear-gradient';
import styled from 'styled-components/native';

export default styled(LienarGradient).attrs({
  colors: ['#572ee1', '#7159c1'],
})`
  flex: 1;
`;
